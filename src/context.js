import { createContext } from "react";

const ThemeContext = createContext(null);
const CurrentUserContext = createContext(null);
const NewsContext = createContext(null);
export { ThemeContext, CurrentUserContext, NewsContext };
